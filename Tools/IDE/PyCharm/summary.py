ctrl-sh-a
#find actions
ctrl-alt-r
#tools-> run manage.py task
можно просматривать фаилы бд просто открыв их в проекте
при переключении acejump на qwerty -> restart pycharm

editor
    ->general
        caret movement
            when moving by words -> stop at both word boundaries
    ->autoimport
        python
            show import popup
    ->code folding
        collapse by default
            file headers
            imports
colorsheme -> diff & merge отвечает за цвета рядом со scrollbars

^c
#copy line

paste line selection always below

^enter
вставить строку после без перехода на нее

hippie completion -> корректно исправляет язык даже с кастомными раскладками!

^+f
#find text

alt + ` (eng layout) => VCS popup menu

при указании authour в vcs имеет формат name email
пкм на корне -> add to git
vcs add remote=>push
